<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <property name="PATTERN" value="%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n" />

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Isolating custom package logs -->
    <!-- 'additivity="false"' ensures we don't duplicate logs if the root also has the same appender -->
    <logger name="dev.ayam.learn.logback.custom" level="ALL" additivity="false">
        <appender-ref ref="CONSOLE" />
    </logger>

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>

</configuration>