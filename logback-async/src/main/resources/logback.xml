<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <property name="LOG_DIR" value="logs" />
    <property name="PATTERN" value="%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n" />

    <!-- 1. Console Appender (The 'Worker' appender) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- 2. File Appender (Another 'Worker' appender) -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>${LOG_DIR}/async-demo.log</file>
        <encoder>
            <pattern>${PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- 3. Async Appender (The 'Manager' appender) -->
    <!-- It dispatches log events to the worker appenders in a separate thread -->
    <appender name="ASYNC_CONSOLE" class="ch.qos.logback.classic.AsyncAppender">
        <!-- The queue size determines how many events can be buffered before blocking or discarding -->
        <queueSize>512</queueSize>
        <!-- By default, when the queue is 80% full, TIB, DEBUG and INFO events are discarded -->
        <!-- Setting discardingThreshold to 0 means we never discard events -->
        <discardingThreshold>0</discardingThreshold>
        <!-- includeCallerData=true is expensive as it requires capturing the stack trace -->
        <includeCallerData>false</includeCallerData>
        
        <appender-ref ref="CONSOLE" />
    </appender>

    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>1024</queueSize>
        <appender-ref ref="FILE" />
    </appender>

    <root level="INFO">
        <appender-ref ref="ASYNC_CONSOLE" />
        <appender-ref ref="ASYNC_FILE" />
    </root>

</configuration>